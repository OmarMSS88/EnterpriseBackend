apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: anonymous-binding
  namespace: default
subjects:
- kind: User
  name: system:anonymous
roleRef:
  kind: ClusterRole  # or Role if you want to limit to a specific namespace
  name: view  # Adjust the role name based on your requirements
  apiGroup: rbac.authorization.k8s.io
